const Game = ({ verify }) => {
    return (
        <div>
            <h1>Iniciou o jogo!</h1>
            <button onClick={verify}>FINALIZAR JOGO</button>
        </div>
    );
}
export default Game;